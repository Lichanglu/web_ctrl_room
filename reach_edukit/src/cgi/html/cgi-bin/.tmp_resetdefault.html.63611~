        <form enctype="multipart/form-data" id="wmform">
          <table width="753" height="100" border="0">
            <tr>
              <td width="148" height="20" class="$image_prex$itemnametd">&nbsp;</td>
              <td width="565"><label><h5>$resetdefault.title$</h5></label></td>
            </tr>
            <!--tr>
              <td height="54" class="$image_prex$itemnametd">&nbsp;</td>
              <td><div class="resetdefault"><h5>Note: </h5>The device will reboot soon !</div></td>
            </tr-->
            <tr>
              <td height="35">&nbsp;</td>
              <td>
                <input style="margin-left:0px;" id="restoreBtn" type="button" class="click_btn_long" value="$resetdefault.restore$" />
                <input id="rebootBtn" type="button" class="$image_prex$click_btn" value="$resetdefault.reboot$" style="letter-spacing:0px;padding-left:0px;" />
              </td>
            </tr>
          </table>
        </form>
<script>
jQuery(function() {
	initFormValidation();
	
	jQuery('#restoreBtn').click(function() {
		art.dialog({
			title: '$resetdefault.confirm$',
			okValue: '$resetdefault.ok$',
			cancelValue: '$resetdefault.cancel$',
			lock: true,
			fixed: true,
		    background: '#600', // 背景色
		    opacity: 0.87,	// 透明度
		    content: "$resetdefault.restoreconfirm$",
		    ok: function() {
				var data = {"actioncode": "418"};
				jQueryAjaxCmd({
					"data": data,
					success: function(ret) {}
				});
				window.setTimeout('window.location.href = "encoder.cgi"', 1000);
			//	window.location.href = "encoder.cgi";
		    },
		    cancel: true
		});
	});
	
	jQuery('#rebootBtn').click(function() {
		art.dialog({
			title: '$resetdefault.confirm$',
			okValue: '$resetdefault.ok$',
			cancelValue: '$resetdefault.cancel$',
			lock: true,
			fixed: true,
		    background: '#600', // 背景色
		    opacity: 0.87,	// 透明度
		    content: "$resetdefault.rebootconfirm$",
		    ok: function() {
				var data = {"actioncode": "215"};
				jQueryAjaxCmd({
					"data": data,
					success: function(ret) {}
				});
				//window.location.href = "encoder.cgi";
				window.setTimeout('window.location.href = "encoder.cgi"', 1000);
		    },
		    cancel: true
		});
	});
	
	
});
</script> 